{% extends 'base.html.twig' %}

{% block title %}AvBooking{% endblock %}

{% block body %}
    <h1>AvBooking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_booking.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_booking_index') }}">back to list</a>

    <a href="{{ path('app_av_booking_edit', {'id': av_booking.id}) }}">edit</a>

    {{ include('av_booking/_delete_form.html.twig') }}
{% endblock %}
