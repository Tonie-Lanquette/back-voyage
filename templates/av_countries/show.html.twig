{% extends 'base.html.twig' %}

{% block title %}AvCountries{% endblock %}

{% block body %}
    <h1>AvCountries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ av_country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_countries_index') }}">back to list</a>

    <a href="{{ path('app_av_countries_edit', {'name': av_country.name}) }}">edit</a>

    {{ include('av_countries/_delete_form.html.twig') }}
{% endblock %}
