{% extends 'base.html.twig' %}

{% block title %}AvCategories{% endblock %}

{% block body %}
    <h1>AvCategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ av_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_categories_index') }}">back to list</a>

    <a href="{{ path('app_av_categories_edit', {'name': av_category.name}) }}">edit</a>

    {{ include('av_categories/_delete_form.html.twig') }}
{% endblock %}
