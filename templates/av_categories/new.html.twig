{% extends 'base.html.twig' %}

{% block title %}New Categories{% endblock %}

{% block body %}
{% if is_granted('ROLE_ADMIN') %}
   <div class="flex flex-col w-full h-5/6 items-center justify-center "> 
    <div class="w-1/3 dark:bg-zinc-800 bg-white px-10 py-16 rounded-lg">
        <h1 class="text-center font-bold mb-10">Create categories</h1>   
{{ include('av_categories/_form.html.twig') }}
    </div> 
    <div class="rounded bg-zinc-500 w-fit px-4 py-2 my-4">
        <a href="{{ path('app_av_categories_index') }}">back to list</a>
    </div>
</div>
{% else %}
<script>window.location.href = "{{ path('app_av_travels_index') }}";</script>
{% endif %}

    

   
{% endblock %}
