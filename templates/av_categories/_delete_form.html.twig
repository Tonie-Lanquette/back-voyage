<form method="post" action="{{ path('app_av_categories_delete', {'name': av_category.name}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_category.id) }}">
    <button class="btn">Delete</button>
</form>
