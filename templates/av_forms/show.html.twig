{% extends 'base.html.twig' %}

{% block title %}AvForms{% endblock %}

{% block body %}
    <h1>AvForms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_form.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ av_form.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_forms_index') }}">back to list</a>

    <a href="{{ path('app_av_forms_edit', {'id': av_form.id}) }}">edit</a>

    {{ include('av_forms/_delete_form.html.twig') }}
{% endblock %}
