<form method="post" action="{{ path('app_av_forms_delete', {'id': av_form.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_form.id) }}">
    <button class="btn">Delete</button>
</form>
