{% extends 'base.html.twig' %}

{% block title %}AvStatus{% endblock %}

{% block body %}
    <h1>AvStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ av_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ av_status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_av_status_index') }}">back to list</a>

    <a href="{{ path('app_av_status_edit', {'name': av_status.name}) }}">edit</a>

    {{ include('av_status/_delete_form.html.twig') }}
{% endblock %}
