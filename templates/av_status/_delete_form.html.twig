<form method="post" action="{{ path('app_av_status_delete', {'name': av_status.name}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ av_status.id) }}">
    <button class="btn">Delete</button>
</form>
